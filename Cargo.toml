[package]
name = "sassenachfixer"
version = "0.1.0"
edition = "2024"
description = "A cheeky Scots <-> English translator"
license = "MIT"

[dependencies]
clap = {version = "4", features = ["derive"]}
serde = {version = "1", features = ["derive"]}
serde_json = "1"
